# 决策记录

- M1 使用模块化单体，不提前拆微服务。
- 规则配置采用 YAML，结果持久化并记录版本。
- 默认敏感距离仅提供小学/中学 200 米示例规则，其他场所统一要求当地政策复核。
- 本地与测试允许 SQLite；生产只使用 PostgreSQL/PostGIS 和 Alembic。
- M1 地图区域提供定位和坐标展示；在线高德底图作为下一小版本集成，避免把 Web Service Key 暴露到前端。
